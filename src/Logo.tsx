import { Typography } from '@mui/material';
import React from 'react';
const Logo = () => (
  <Typography variant='h1' style={{textTransform:"none", color:"#000"}}>DataStake</Typography>
);

export default Logo;